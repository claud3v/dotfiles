# Add `~/bin` to the `$PATH`
export PATH="$HOME/bin:$PATH";

source "$HOME/.aliases"
source "$HOME/.functions"
source "$HOME/.git_functions"

# it has to be the last file to load
source "$HOME/.tools_setup"